# This directory is for an optional pre-trained XGBoost meta-classifier.
# If models/meta_classifier.pkl is absent, the pipeline uses a heuristic ensemble.
# 
# To train and save the XGBoost model from Colab, see the training notebook
# (train_meta_classifier.ipynb — run after collecting signal scores from HaluEval/FactCC).
